{
   "service": {
     "name": "monitor",
     "tags": ["tagmonitor"],
     "port": 9090,
     "check": {
       "id": "prometheus",
       "name": "prometheus server",
       "http": "http://localhost:9090",
       "method": "GET",
       "interval": "10s",
       "timeout": "1s"
     }
    }
}

